#pragma once

#include <cstddef>

namespace UniformGeneratorHelper {

// Returns the radical inverse in the provided base for non-negative index n.
double RadicalInverse(unsigned int base, size_t n);

}  // namespace UniformGeneratorHelper
